type Note @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  image: String
}

type Article @model @auth(rules: [{allow: public}]) {
  id: ID!
  author: String!
  title: String
  content: String
  date: AWSDate
}
 